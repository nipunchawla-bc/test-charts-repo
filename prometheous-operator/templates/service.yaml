apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheous-operator.fullname" . }}
  labels:
    {{- include "prometheous-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheous-operator.selectorLabels" . | nindent 4 }}
