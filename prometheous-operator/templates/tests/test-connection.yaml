apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheous-operator.fullname" . }}-test-connection"
  labels:
    {{- include "prometheous-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheous-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
